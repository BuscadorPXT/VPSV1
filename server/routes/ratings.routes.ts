
import express, { Router } from 'express';

const router = Router();

// Rating functionality has been completely removed
// This file is kept for compatibility but no longer handles any routes

export default router;
